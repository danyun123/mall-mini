# 经验教训知识库

## 类型字段注释

- 业务 `interface`、`type` 对象结构和页面方法声明不能只写字段类型；每个字段或方法都要补充能让接手者一眼理解含义的中文注释。
- 注释要说明字段在当前业务里的真实用途、展示含义、状态边界或副作用，不只重复字段名。
- 页面私有类型仍然就近维护，但同样需要完整注释；尤其是表单状态、错误状态、触达状态、提交结果和页面事件方法。
- 新增类型时先检查注释完整性，再运行类型检查和测试，避免把“能编译但难理解”的代码交付出去。
